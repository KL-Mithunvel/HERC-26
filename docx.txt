sudo apt update && sudo apt full-upgrade -y
sudo apt install -y git curl wget vim tmux htop unzip zip \
  python3 python3-pip python3-venv python3-dev \
  build-essential cmake pkg-config \
  i2c-tools minicom screen
sudo apt-get install i2c-tools
sudo apt install -y python3-lgpio


sudo reboot
ssh-keygen -t ed25519 -C "klm@smtw.in"
ls ~/.ssh
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
cat ~/.ssh/id_ed25519.pub
ssh -T git@github.com
git config --global user.name "KL-Mithunvel"
git config --global user.email "klm@smtw.in"
git config --global --list

git clone https://github.com/KL-Mithunvel/HERC-26
cd HERC-26/
python -m venv .venv
